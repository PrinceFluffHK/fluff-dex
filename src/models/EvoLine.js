import React from "react";

class EvoLine {
    constructor (line, lineType) {
        this.line = line
        this.lineType = lineType
    }

    display() {
        if (this.lineType === 0) {
            
        }
    }
}