import { Link } from 'react-router-dom';
import React from 'react';

const TopBar = (props) => {
    return (
        <div className='top-bar'>
            <Link to="/">Home</Link>
            
        </div>
    )
}

export default TopBar